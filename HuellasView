import SwiftUI

struct HuellasView: View {
    var body: some View {
        VStack {
            // Título
            Text("Tus Huellas")
                .font(.title)
                .padding()
                .foregroundColor(.white)
                .frame(maxWidth: .infinity)
                .background(Color.blue)
            
            // Grilla de huellas
            VStack(spacing: 20) {
                HStack(spacing: 20) {
                    HuellaCard(title: "Hídrica", value: 1375, imageName: "grafica_hidrica")
                    HuellaCard(title: "Energética", value: 1375, imageName: "grafica_energetica")
                }
                HStack(spacing: 20) {
                    HuellaCard(title: "Carbono", value: 1375, imageName: "grafica_carbono")
                    HuellaCard(title: "Residuos", value: 1375, imageName: "grafica_residuos")
                }
            }
            .padding(.top, 40)
            
            // Botón de recomendaciones
            Button(action: {
                // Acción para recomendaciones
            }) {
                Text("Recomendaciones")
                    .foregroundColor(.white)
                    .padding()
                    .frame(maxWidth: .infinity)
                    .background(Color.blue)
                    .cornerRadius(10)
                    .padding(.top, 40)
            }
            .padding(.horizontal, 40)
            
            Spacer()
            
            // Barra de pestañas inferior (Simulada)
            HStack {
                Spacer()
                Image(systemName: "chart.bar")
                Spacer()
                Image(systemName: "globe")
                Spacer()
                Image(systemName: "person.circle")
                Spacer()
            }
            .padding()
            .background(Color(.systemGray6))
        }
    }
}

struct HuellasView_Previews: PreviewProvider {
    static var previews: some View {
        HuellasView()
    }
}
